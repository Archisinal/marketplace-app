{
  "name": "archisinal-marketplace-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "backend:start": "yarn workspace @archisinal/backend start",
    "backend:dev": "yarn workspace @archisinal/backend dev",
    "indexer:start": "yarn workspace @archisinal/indexer start",
    "client:start": "yarn workspace @archisinal/client dev",
    "node:deploy": "yarn workspace @archisinal/indexer deploy",
    "db:explorer": "yarn workspace @archisinal/db explorer",
    "db:deploy": "yarn workspace @archisinal/db migrate:deploy"
  },
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
